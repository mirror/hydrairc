--- atlctrls.h	2005-11-29 01:30:02.000000000 +0000
+++ D:\My Documents\projects\hydrairc\source\HydraIRC\include\altered_wtl_includes\7.5\atlctrls.h	2006-05-21 15:57:38.625000000 +0100
@@ -9,6 +9,9 @@
 // the terms of this license. You must not remove this notice, or
 // any other, from this software.
 
+// Hydra's Changes
+// added CToolBarCtrlT::SetImageList(HIMAGELIST hImageList, int nImageList)
+
 #ifndef __ATLCTRLS_H__
 #define __ATLCTRLS_H__
 
@@ -2527,7 +2530,7 @@
 		ATLASSERT(::IsWindow(m_hWnd));
 		return CImageList((HIMAGELIST)::SendMessage(m_hWnd, HDM_SETIMAGELIST, 0, (LPARAM)hImageList));
 	}
-
+	
 	BOOL GetOrderArray(int nSize, int* lpnArray) const
 	{
 		ATLASSERT(::IsWindow(m_hWnd));
@@ -5067,6 +5070,14 @@
 		return CImageList((HIMAGELIST)::SendMessage(m_hWnd, TB_SETIMAGELIST, 0, (LPARAM)hImageList));
 	}
 
+  // Hydra - begin
+	CImageList SetImageList(HIMAGELIST hImageList, int nImageList)
+	{
+		ATLASSERT(::IsWindow(m_hWnd)); 
+		return CImageList((HIMAGELIST)::SendMessage(m_hWnd, TB_SETIMAGELIST, nImageList, (LPARAM)hImageList));
+	}
+  // Hydra - end	
+
 	CImageList GetDisabledImageList() const
 	{
 		ATLASSERT(::IsWindow(m_hWnd));
